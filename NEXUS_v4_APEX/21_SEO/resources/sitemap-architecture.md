## Sitemap architecture (Module 10.1)

[conf: DEFINITIVE]

- Use a **sitemap index** and split sitemaps by type:
  - pages, blog/articles, products, images, video
- Each sitemap: max 50,000 URLs and 50MB.
- Include `lastmod`. Reference the index from `robots.txt`.

