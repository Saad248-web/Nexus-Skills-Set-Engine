## Pillar-cluster topical authority (Module 10.2)

[conf: RECOMMENDED]

- Define pillar pages for each main topic.
- Cluster pages answer specific long-tail queries.
- Internal linking:
  - clusters → pillar (strong)
  - pillar → clusters (curated)
- Avoid orphan pages; every page must have an internal link path.

