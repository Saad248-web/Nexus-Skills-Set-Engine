## Focus management (Module 07)

- Modal open: focus moves inside modal (first focusable or container)
- Modal close: focus returns to trigger
- Add skip-to-content link as first focusable element on every page
- Focus ring is a design element (`:focus-visible`), never remove without replacement
- Focus traps: use `inert` on background for overlays

