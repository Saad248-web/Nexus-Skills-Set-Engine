## Reduced motion first (Module 06)

- Every animation must have a `prefers-reduced-motion: reduce` version.
- Replace position/scale motion with opacity-only fades when reduced motion is requested.
- This is accessibility baseline, not optional.

