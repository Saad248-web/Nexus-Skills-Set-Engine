## Token tiers (Module 05)

Three-level system:

1) Primitive tokens: raw values (spacing, radius, hues)
2) Semantic tokens: meaning (bg, fg, border, primary)
3) Component tokens: component-specific (button padding, card radius)

Components should reference component tokens only.

