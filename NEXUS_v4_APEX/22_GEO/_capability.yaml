engine: 22_GEO
purpose: "GEO engine: AI crawler strategy, citation architecture, entity establishment, llms.txt, AEO patterns."
whenToInvoke:
  - "Invoke when this domain is needed."
provides:
  - "Emits a primary contract artifact."
hardRules:
  - "Use invoke language (REF-03)."
  - "No cross-engine internal path references (PATH-05)."
