## Speculation Rules API (Module 09)

Use the browser’s speculation rules to prefetch/prerender likely navigations.

- Opt-in per route group
- Respect data sensitivity (don’t prerender authenticated pages to shared caches)

