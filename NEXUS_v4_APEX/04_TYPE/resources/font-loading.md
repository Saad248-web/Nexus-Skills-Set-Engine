## Font loading plan (Module 03)

- Preload critical fonts (display/body) when used above-the-fold.
- Use `font-display: swap`.
- Prefer variable fonts when it reduces total requests.
- Always define fallbacks (system stacks).

