## REST rules (Module 12)

- Resource naming: nouns, plural (`/users`, `/orders/{id}/items`)
- Methods: GET safe, PUT idempotent replace, PATCH partial, POST create/action, DELETE idempotent
- Never use GET for side effects

