## tests/

Validation suite will be implemented in Phase 1+.

Goals:

- Ensure every engine declares required footprint (`SKILL.md`, `_genome.yaml`, `_capability.yaml`, `customize.yaml`, contracts, resources, templates)
- Enforce PATH-05 encapsulation and REF-03 invoke-language rules
- Enforce token budgets and tier policy
- Validate HERMES approval/audit/effectiveness tracking behavior

