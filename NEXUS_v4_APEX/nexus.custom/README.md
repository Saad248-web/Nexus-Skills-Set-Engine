## nexus.custom/

Project-level per-engine overrides live here.

- `<ENGINE>.yaml`: team overrides (committed)
- `<ENGINE>.user.yaml`: personal overrides (gitignored)

Example: `03_PALETTE.example.yaml` and `03_PALETTE.user.example.yaml`

