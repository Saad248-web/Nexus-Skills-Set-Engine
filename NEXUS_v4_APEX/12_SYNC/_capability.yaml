engine: 12_SYNC
purpose: "Sync engine: realtime vs async decision tree, offline queues, webhooks reliability, background jobs."
whenToInvoke:
  - "Invoke when this domain is needed."
provides:
  - "Emits a primary contract artifact."
hardRules:
  - "Use invoke language (REF-03)."
  - "No cross-engine internal path references (PATH-05)."
