## Code graph adapter (innovation #22)

Production path: optional `code-review-graph` pip package via `0P_PLUGINS`.

Local fallback:

```bash
node scripts/graph.mjs <file>
```

Heuristic sibling-file blast radius (deterministic, fast).
